# Airtable read-only access (never commit real values)
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_SNAPSHOT_PATH=data/raw/airtable_snapshot.json

# Optional provider selection; mock is safe for offline development
RI_PROVIDER=mock
GEMINI_API_KEY=

